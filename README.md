# PacketPulse---Deep-Packet-Inspection-Engine
A Java-based Deep Packet Inspection (DPI) Engine that analyzes PCAP files, parses network packets, extracts TLS SNI, identifies applications, applies security policies, and generates traffic analysis reports.
